/** @type {import('next').NextConfig} */
const nextConfig = {
    // images: {
    //     remotePatterns: [
    //         {
    //             protocol: "https", // Specify the protocol as a string
    //             hostname: "picsum.protos"
    //         }
    //     ]
    // }
};

export default nextConfig;
